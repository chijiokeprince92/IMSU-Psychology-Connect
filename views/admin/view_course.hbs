<!DOCTYPE html>
<html lang="en-US">

<head>
    {{>head}}
</head>

<body>
    {{>admin_lecbody}}
    <br>
    <h1><b>
            <center>COURSE PAGE</center>
        </b>
    </h1>
    <br><br>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 col-sm-12 col-md-8 col-lg-7">
                <p><b>Course_Code:</b> {{kind.coursecode}}</p>
                <p><b>Course_Title: </b> {{kind.coursetitle}}</p>
                <p><b>Level: </b> {{kind.level}}</p>
                <p><b>Semester:</b> {{kind.semester}}</p>
                <p><b>Units: </b>{{kind.units}}</p>
                <p><b>Borrowed_Course: </b>{{kind.borrowed}}</p>
                <p><b>Lecturer: </b><a href="/staffprofile/{{teacher.id}}">{{teacher.name}}</a></p>
            </div>
        </div>
    </div>
    <br>
    <h3><b>
            <center>COURSE OUTLINE</center>
        </b></h3>
    <br>
    {{#if kind.courseoutline}}
        <p>{{kind.courseoutline}}</p>
    {{else}}
        <p>COURSE OUTLINE NOT AVAILABLE</p>
    {{/if}}
    <br>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-4">
                <a href="/addcourseoutline/{{kind.id}}" type="button" class="btn btn-info btn-lg">EDIT COURSE</a>
            </div>
            <div class="col-sm-4">
                <form action="/deletecourse/{{kind.id}}" method="POST">
                    <input type="hidden" />
                    <button type="submit" class="btn btn-danger">DELETE COURSE</button>
                </form>
            </div>
        </div>
    </div>
    <br><br>
    <footer>{{>footer}}</footer>
</body>

</html>