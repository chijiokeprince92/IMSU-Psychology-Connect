<!DOCTYPE html>
<html>

<head>
    {{>head}}
</head>

<body class="body">
    <img src="/images/drizzys.jpg" alt="NAPS image" class="logo" />
    {{#if allowed}}
        {{>student_lecbody}}
    {{else if admin}}
        {{>admin_lecbody}}
    {{else if staff_session}}
        {{>staff_lecbody}}
    {{else}}
        {{>view_body}}
    {{/if}}
    <br>
    {{{body}}}
    <footer>
        {{>footer}}
    </footer>
</body>

</html>